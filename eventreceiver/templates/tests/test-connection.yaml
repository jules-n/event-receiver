apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eventreceiver.fullname" . }}-test-connection"
  labels:
    {{- include "eventreceiver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eventreceiver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
